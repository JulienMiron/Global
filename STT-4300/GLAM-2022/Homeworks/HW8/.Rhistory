par(mfrow =c(1,1))
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='hat',
tcc=1.55)
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
tcc=1.55)
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
tcc=1.55,trace.lev = T)
install.packages("robustbase")
rm(list=ls()) # clear the environnment of all objects defined previously
food <- read.table(file='foodstamp.dat',header=T)
str(food)
library(robustbase)
food$lincome <- log(food$Income+1)
par(mfrow=c(1,2))
hist(food$Income,col='deepskyblue1',nclass=20,prob=T)
lines(density(food$Income),col='red',lwd=2)
hist(food$lincome,col='deepskyblue3',nclass=20,prob=T)
lines(density(food$lincome),col='red',lwd=2)
par(mfrow=c(1,1))
min(food$Income) # explains lonely point in log(Income+1)
pairs(food)
par(mfrow=c(1,3))
plot(as.factor(Participation)~as.factor(Tenancy), data=food)
plot(as.factor(Participation)~as.factor(Suppl.Income), data=food)
plot(as.factor(Participation)~lincome, data=food)
par(mfrow=c(1,1))
pairs(food)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
tcc=1.55)
str(food)
summary(food.robglm.155)
summary(food.robglm.155)
summary(residuals(food.robglm.155, type = "deviance"))
# deviance standardized residuals
summary(residuals(food.robglm.155, type = "pearson"))
plot(food.robglm.155$resid) # one very large outlier
which.min(food.robglm.155$resid) # obs. 5
food[5,]
colvec<- rep(1,dim(food)[[1]])
colvec[5] <- 2 # give color red to obs. 5
plot(food.robglm.155$w.r,
main='Robustness weights',
col=colvec,pch=19)
# = > obs. 5 has weight (on resid) very close to 0
food.robglm.155$w.r[5]
summary(food.robglm.155$w.x)
summary(food.robglm.155)
plot(food.robglm.155$w.x,col=colvec,pch=19, main="weights on x_i")
plot(food.robglm.155)
par(mfrow =c(1,3))
plot(residuals(food.robglm.155, type = "pearson"),
main = "Residuals vs Index")
abline(h=c(-1.96, 1.96) , col = "blue", lty = 2)
plot(x = fitted(food.robglm.155), y = residuals(food.robglm.155, type = "pearson"), main= "Residuals vs fitted")
abline(h=c(-1.96, 1.96) , col = "blue", lty = 2)
which(fitted(food.robglm.155)==max(fitted(food.robglm.155)))
qqnorm(residuals(food.robglm.155)) ; abline(a=0,b=1)
par(mfrow =c(1,1))
# not great fit nevertheless, probably partly due to covariates.
plot(fitted(food.robglm.155),y = food$Participation)
food.robglm.40 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='none',
tcc=40)
food.glm <- glm(Participation~Tenancy+Suppl.Income+lincome,data=food,family=binomial)
data.frame(
tcc.40 = summary(food.robglm.40)$coef[,c(1,2)],
glm = summary(food.glm)$coef[,c(1,2)]  # the same
)
data.frame(
tcc.155 = summary(food.robglm.155)$coef[,c(1,2)],
glm = summary(food.glm)$coef[,c(1,2)]
)
food.robglm.155.2 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
tcc=1.55) # without the weight on X
summary(food.robglm.155)$coef
summary(food.robglm.155)$coef   # lincome is significant
summary(food.robglm.155.2)$coef # not any more
food.robglm.155.3 <- glmrob(Participation~Tenancy+lincome,data=food,
family=binomial,weights.on.x='robCov',tcc=1.55)
summary(food.robglm.155.3)$coef
plot(food.robglm.155.3$resid)
plot(food.robglm.155.3$resid[-5])
anova(food.robglm.155.3,food.robglm.155,test="QD")
food <- read.table(file='foodstamp.dat',header=T)
str(food)
food$lincome <- log(food$Income+1)
par(mfrow=c(1,2))
hist(food$Income,col='deepskyblue1',nclass=20,prob=T)
lines(density(food$Income),col='red',lwd=2)
hist(food$lincome,col='deepskyblue3',nclass=20,prob=T)
lines(density(food$lincome),col='red',lwd=2)
par(mfrow=c(1,1))
min(food$Income) # explains lonely point in log(Income+1)
pairs(food)
par(mfrow=c(1,3))
plot(as.factor(Participation)~as.factor(Tenancy), data=food)
plot(as.factor(Participation)~as.factor(Suppl.Income), data=food)
plot(as.factor(Participation)~lincome, data=food)
par(mfrow=c(1,1))
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
tcc=1.55)
library(robustbase)
summary(food.robglm.155)
library(robustbase)
food <- read.table(file='foodstamp.dat',header=T)
str(food)
food$lincome <- log(food$Income+1)
par(mfrow=c(1,2))
hist(food$Income,col='deepskyblue1',nclass=20,prob=T)
lines(density(food$Income),col='red',lwd=2)
hist(food$lincome,col='deepskyblue3',nclass=20,prob=T)
lines(density(food$lincome),col='red',lwd=2)
par(mfrow=c(1,1))
min(food$Income) # explains lonely point in log(Income+1)
pairs(food)
par(mfrow=c(1,3))
plot(as.factor(Participation)~as.factor(Tenancy), data=food)
plot(as.factor(Participation)~as.factor(Suppl.Income), data=food)
plot(as.factor(Participation)~lincome, data=food)
par(mfrow=c(1,1))
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
tcc=1.55) # tcc, robustness and efficiency
# Alternatively, weights on X can be based on the "Hat" matrix. (Then related to the leverage).
# food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
#                           data=food,
#                           family=binomial,
#                           weights.on.x='hat',
#                           tcc=1.55)
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.345)) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
summary(food.robglm.155)
glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
control = list(tcc = 1.55)) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
tcc = 1.55) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-09, test.acc = "coef", maxit = 50, tcc = 1.55) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-09, test.acc = "coef", maxit = 50, tcc = 1.55) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-09, test.acc = "coef", maxit = 50, tcc = 1.55) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-09, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-09, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~as.factor(Tenancy)+as.factor(Suppl.Income)+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='none',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-10, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='hat',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='hat',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='hat',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='hat',
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
rm(list=ls()) # clear the environnment of all objects defined previously
library(robustbase)
food <- read.table(file='foodstamp.dat',header=T)
str(food)
food$lincome <- log(food$Income+1)
str(food)
par(mfrow=c(1,2))
hist(food$Income,col='deepskyblue1',nclass=20,prob=T)
lines(density(food$Income),col='red',lwd=2)
hist(food$lincome,col='deepskyblue3',nclass=20,prob=T)
lines(density(food$lincome),col='red',lwd=2)
par(mfrow=c(1,1))
min(food$Income) # explains lonely point in log(Income+1)
pairs(food)
par(mfrow=c(1,3))
plot(as.factor(Participation)~as.factor(Tenancy), data=food)
plot(as.factor(Participation)~as.factor(Suppl.Income), data=food)
plot(as.factor(Participation)~lincome, data=food)
par(mfrow=c(1,1))
food.robglm.155 <- glmrob(Participation~Tenancy+Suppl.Income+lincome,
data=food,
family=binomial,
weights.on.x='robCov', # don't use Mahalanobis with multimodal variables.
# control = glmrobMqle.control(acc = 1e-04, test.acc = "coef", maxit = 50, tcc = 1.55)) # tcc, robustness and efficiency
acc = 1e-4, test.acc = "coef", maxit = 50, tcc = 1.55,
trace.lev = T) # tcc, robustness and efficiency
summary(food.robglm.155)
summary(residuals(food.robglm.155, type = "deviance"))
# deviance standardized residuals
summary(residuals(food.robglm.155, type = "pearson"))
plot(food.robglm.155$resid) # one very large outlier
which.min(food.robglm.155$resid) # obs. 5
food[5,]
colvec<- rep(1,dim(food)[[1]])
colvec[5] <- 2 # give color red to obs. 5
plot(food.robglm.155$w.r,
main='Robustness weights',
col=colvec,pch=19)
# = > obs. 5 has weight (on resid) very close to 0
food.robglm.155$w.r[5]
summary(food.robglm.155$w.x)
plot(food.robglm.155$w.x,col=colvec,pch=19, main="weights on x_i")
summary(food.robglm.155)
